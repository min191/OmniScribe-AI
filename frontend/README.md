# OmniScribe frontend

React/Vite workspace for upload, progressive OCR review, metadata editing, and Obsidian export.

```powershell
Copy-Item .env.example .env
npm install
npm run dev
```

Use `VITE_API_BASE_URL` to point the UI at a non-default backend.

